source "https://rubygems.org"

ruby '2.5.0' # Or whatever the latest Ruby version is

gem 'sinatra'
gem 'pg'
gem 'envyable'
gem 'sinatra-flash'

group :test do
  gem 'rake'
  gem 'capybara'
  gem 'rspec'
  gem 'rubocop' # Optional
  gem 'simplecov', require: false # Optional
  gem 'simplecov-console', require: false # Optional
end
